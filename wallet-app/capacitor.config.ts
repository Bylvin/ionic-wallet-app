import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.itdp.IDEAWallet',
  appName: 'IDEAWallet',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
